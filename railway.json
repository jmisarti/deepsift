{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "if [ \"$DEEPSIFT_PROCESS_ROLE\" = \"sms_attempt_sync\" ]; then python scripts/reisift_sms_attempt_worker.py; else gunicorn -w 1 -k gthread --threads 8 -b 0.0.0.0:$PORT app:app; fi",
    "healthcheckPath": "/healthz",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
